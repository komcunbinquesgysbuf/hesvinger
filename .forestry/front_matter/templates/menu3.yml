---
label: menu3
hide_body: true
fields:
- type: field_group_list
  name: Main menu
  label: Main menu
  fields:
  - type: text
    name: title
    label: title
  - type: text
    name: url
    label: url
  - name: type
    type: select
    default: []
    config:
      required: false
      options:
      - internal
      - external
      source:
        type: simple
        section: 
        file: src/data/main_menu.json
        path: 
    label: type
  config:
    labelField: title
